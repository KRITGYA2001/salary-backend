# Copy to .env and adjust. .env is git-ignored.
VM_HOST=your.vm.host.or.ip
VM_USER=ubuntu
# Path to the private key (never commit the key itself)
SSH_KEY_PATH=~/.ssh/your_vm_key
# JDK used to build (leave empty to use JAVA_HOME / PATH)
BUILD_JAVA_HOME=""
